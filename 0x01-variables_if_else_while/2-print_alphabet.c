#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: to print alphabets in c
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
		putchar('\n');

	return (0);
}
