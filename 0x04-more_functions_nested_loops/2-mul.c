#include "main.h"

/**
 * mul - returns the multiplication of 2 digits.
 * @a: 1st digit
 * @b; 2nd digit
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
